{
  "name": "app",
  "private": "true",
  "dependencies": {
    "@hotwired/stimulus": "^3.2.1",
    "@hotwired/turbo-rails": "^7.3.0",
    "esbuild": "^0.18.4",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --sourcemap --minify --outdir=app/assets/builds --public-path=/assets --loader:.ts=ts --target=es2020",
    "ts-check": "tsc --project tsconfig.json --noEmit --watch --preserveWatchOutput"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0"
  }
}
